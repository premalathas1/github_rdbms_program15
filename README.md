Objective
Write a PL/SQL program using an IF–ELSE statement to determine whether a student has passed or failed based on their marks.

Problem Statement
Write a PL/SQL program that:

Accepts a student's marks as input.

Checks whether the marks are greater than or equal to 40.

Displays:

PASS if the marks are 40 or above.

FAIL if the marks are below 40.
